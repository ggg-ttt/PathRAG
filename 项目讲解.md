# PathRAG项目详细讲解文档
## 1. 项目概述
PathRAG是一个基于关系路径的图检索增强生成(Graph RAG)项目，它通过构建知识图谱来增强大语言模型的检索能力。该项目的核心特点是利用实体间的关系路径进行上下文检索，从而提供更精确、更相关的信息来增强模型输出。

主要功能：

- 自动从文档中提取实体和关系并构建知识图谱
- 支持多种存储后端（内存存储、文件存储、外部数据库）
- 基于实体关系的混合检索模式
- 路径查找和关系推理
- 支持多种大语言模型接口
## 2. 核心代码文件及功能
### 2.1 PathRAG.py
这是项目的核心文件，定义了PathRAG主类，负责协调整个系统的工作流程。

主要功能：

- 系统初始化与配置管理
- 多存储后端的创建与协调
- 文档插入处理流程
- 查询处理流程
- 实体删除功能
关键方法：

- __init__ ：初始化系统，配置工作目录、存储后端、LLM模型等
- insert ：插入文档内容，包含分块、实体提取、知识图谱构建等流程
- query ：处理用户查询，支持多种查询模式
- delete_by_entity ：根据实体名称删除相关的实体和关系
- ainsert_custom_kg ：支持自定义知识图谱的插入
### 2.2 base.py
定义了系统的基础数据结构和抽象基类，为存储层提供统一接口。

主要内容：

- QueryParam ：查询参数数据类，定义查询模式、上下文长度等
- BaseVectorStorage ：向量存储抽象基类，定义向量检索相关接口
- BaseKVStorage ：键值存储抽象基类，定义键值操作接口
- BaseGraphStorage ：图存储抽象基类，定义知识图谱操作接口
- TextChunkSchema ：文档分块的数据结构定义
### 2.3 storage.py
实现了各种存储后端的具体实现类。

主要实现：

- JsonKVStorage ：基于JSON文件的键值存储实现
- NanoVectorDBStorage ：基于NanoVectorDB的向量存储实现
- NetworkXStorage ：基于NetworkX的图存储实现
- 延迟导入的外部存储：Neo4JStorage、OracleKVStorage等
### 2.4 llm.py
实现了各种大语言模型的接口适配。

主要实现：

- OpenAI接口（GPT系列模型）
- Azure OpenAI接口
- Bedrock接口（Amazon的模型）
- 重试机制和错误处理
### 2.5 operate.py
实现了系统的核心操作逻辑，包括文档处理、实体提取、查询处理等。

主要功能：

- chunking_by_token_size ：根据token大小对文档进行分块
- extract_entities ：从文档分块中提取实体和关系
- kg_query ：执行知识图谱查询
- 路径查找算法： find_paths_and_edges_with_stats 、 bfs_weighted_paths
- 上下文构建和相关度计算
### 2.6 prompt.py
存储了系统中使用的各种提示词模板。

主要内容：

- 实体提取提示词
- 关系提取提示词
- RAG回答生成提示词
- 关键词提取提示词
- 示例数据格式
### 2.7 utils.py
提供了各种工具函数，支持系统的其他组件。

主要功能：

- 日志管理
- JSON处理
- 哈希计算
- 嵌入函数包装
- 缓存机制
- Token编码解码
- 文本清洗和处理
- 余弦相似度计算
## 3. 系统架构
PathRAG采用模块化设计，主要包括以下几个核心模块：

1. 核心层 ：PathRAG主类，协调整个系统
2. 存储层 ：提供多种存储后端实现，支持键值存储、向量存储和图存储
3. LLM层 ：提供统一的大语言模型接口
4. 操作层 ：实现文档处理、实体提取、查询处理等核心操作
5. 工具层 ：提供各种辅助功能
系统采用异步编程模式，使用asyncio进行并发处理，提高了处理效率。

## 4. 主要流程
### 4.1 文档插入流程
1. 接收文档内容
2. 对文档进行分块处理（基于token大小）
3. 调用LLM从分块中提取实体和关系
4. 将实体和关系存入相应的存储后端
5. 更新知识图谱
### 4.2 查询处理流程
1. 接收用户查询
2. 根据查询参数确定查询模式（混合、仅图、仅向量）
3. 提取查询中的关键词和实体
4. 在知识图谱中查找相关实体和关系
5. 构建上下文信息
6. 调用LLM生成回答
## 5. 关键技术点
### 5.1 实体和关系提取
使用LLM（如GPT-4系列）从文本中提取实体和关系，支持多种实体类型，并能评估关系强度。提取过程采用多轮对话方式，确保尽可能多地提取实体和关系。

### 5.2 知识图谱构建
基于提取的实体和关系构建有向图，支持实体和关系的合并、去重和增强。图中存储实体描述、关系描述、关系强度等信息。

### 5.3 路径查找算法
实现了加权BFS算法，用于在知识图谱中查找实体间的相关路径，支持阈值过滤和权重计算。

### 5.4 混合检索模式
支持多种检索模式：

- 混合模式：结合图检索和向量检索
- 仅图模式：只使用知识图谱进行检索
- 仅向量模式：只使用向量相似度进行检索
### 5.5 缓存机制
实现了查询缓存和结果缓存，提高系统响应速度，支持相似度计算和缓存命中判断。

## 6. 快速开始
根据v1_test.py文件，使用PathRAG的基本步骤如下：

1. 设置OpenAI API密钥
2. 初始化PathRAG实例
3. 插入文档内容
4. 执行查询
```
from PathRAG import PathRAG, QueryParam
from PathRAG.llm import gpt_4o_mini_complete

# 设置API密钥
import os
os.environ["OPENAI_API_KEY"] = "your_api_key"

# 初始化PathRAG
rag = PathRAG(
    working_dir="working_dir",
    llm_model_func=gpt_4o_mini_complete,
)

# 插入文档
with open("text.txt") as f:
    rag.insert(f.read())

# 执行查询
result = rag.query("你的问题", 
param=QueryParam(mode="hybrid"))
print(result)
```
## 7. 配置参数
主要配置参数：

- working_dir ：工作目录，用于存储数据文件
- chunk_token_size ：文档分块的最大token数
- embedding_func ：嵌入函数，用于生成文本向量
- llm_model_func ：LLM模型函数，用于实体提取和回答生成
- entity_extract_max_gleaning ：实体提取的最大轮数
## 8. 总结
PathRAG项目通过结合知识图谱和向量检索技术，为大语言模型提供了更精确、更相关的上下文信息，从而增强了模型的回答质量。项目的模块化设计和多种存储后端支持使其具有良好的扩展性和适应性。

与传统RAG相比，PathRAG的主要优势在于能够利用实体间的关系路径进行更深入的信息检索，从而在处理复杂问题和需要关联信息的场景中表现更好。